## Learned User Preferences

- **Do not talk to the user in more than 1 sentence at a time unless fully required; when it is required, you can use up to 5 sentences.**
- Prefers simple English; user is a PM transitioning to consulting.
- Build the full site in one pass — no v1/v2 phasing; Next.js App Router confirmed.
- Exclude Bible for Bad People and Kotak Securities from public listings; preserve legacy social links; footer brand icons via Font Awesome `<i class="fa-brands">` HTML (not react-fontawesome) with legacy inline SVGs for Giphy and Medium in `lib/legacy-social-icons.ts` (not in FA free / FA 7 changed Medium); `SocialIcon.tsx` adds `.home__footer-icon--giphy` (~1.16× scale) and `.home__footer-icon--medium` (~1.06×) for visual parity with FA icons.
- Prefers agent execute deployment, setup, QA (`/gstack-qa`), and design review (`/gstack-design-review`) itself — not ask user to manually test or follow step-by-step instructions; use Composer (not Sonnet) for subagents.
- Abandoned rashOS scroll-journey, soft-brutalism, and separate Expression subdomain/site — wants extreme simplicity instead ("fuck all this"); Expression and OnDevice should open in-place via Apple HIG bottom sheet on phone and split view on desktop/iPad (use left whitespace; sheet slides up, split enters from left — mindful motion); split view open/close must stay within the allotted left column — right project list must not shift or get overlapped; desktop `.home__primary` capped at tagline width (`--home-measure-narrow` / 34ch) — detail panel must not bleed or expand into project list; split-view dismiss must feel HIG-compliant — one coordinated beat (grid, height, fade in parallel), not fade-then-snap or sequential accordion-then-panel close; not as separate page navigation; `/expression` and `/ondevice` routes may remain for SEO/direct links; static section bodies, no accordion dropdowns in overlays; prefer CSS layout tokens over hardcoded ch/rem in styles — `--home-measure-narrow`, `--home-measure-body`, `--home-detail-viewport-offset` in `tokens.css`.
- Homepage layout: near-white eggshell background; header at all breakpoints with Raashi Shah (Spotify track easter egg, old rose on hover) plus contact line `email me` / `or` / `let's meet sometime` (Calendly); Twitter in footer not header; footer social order LinkedIn → Twitter → GitHub → Medium → Spotify → SoundCloud → Duolingo → Giphy (email/Calendly header only); mobile footer icons in 8-column equal grid (labels visually hidden, 44px touch targets) with meta row below (coral mark then `footerDiscoveryHint` — current copy `something's hidden somewhere here` in `content/site.ts` — nudging visitors to the header name easter egg; caption-scale muted; left-aligned within home padding — no `margin-inline-start: auto` on `.home__footer-meta`); desktop/iPad (≥40rem) footer keeps social links and meta on one row (links left, meta right) — not separate rows; desktop footer text labels only; `.home__footer-mark` uses `public/img/footer-mark.svg` (cropped viewBox from favicon), `1.0625rem` square (`object-fit: contain`), `translateY(-0.06em)` for optical alignment with hint text; no HTML width/height on `SiteShell` img; no dropdown body indent on phone; 68rem max-width shell; responsive grid; no preloader, scroll journey, Lenis, custom cursor, skip-to-content link, or decorative chrome; favicon legacy rose coral SVG at `public/img/favicon.svg` (#C08081); OG share image is static Figma PNG at `app/opengraph-image.png` (1200×630) — code-generated OG abandoned.
- Typography and links: self-hosted Satoshi variable font sitewide; body 400, UI labels medium (500); intro role muted tertiary, tagline at headline scale; readable clamp() sizes; dropdown body inline links (`.home__inline-link`) grey, medium weight, no underline, typographic Satoshi `↗` via `ExternalLinkArrow` (~1–2pt larger, baseline-aligned inline with text — not inline SVG or thick stroke icons), always old rose (`--accent` via `.home__inline-link-icon`); Expression/OnDevice detail links use `InlineLinkArrow` — ↑ on phone, → on desktop/iPad (≥40rem); link text old rose on hover/focus; grouped multi-link `·` separators use old rose accent (`.home__inline-separator`); other internal page links keep ↗ but navigate in same tab (`next/link`, no `target="_blank"`); header/footer links keep their own styles.
- Homepage intro copy from `siteConfig` in `lib/metadata.ts`: `introRole` "Product Manager, AI Engineer" + `introTagline` "Scoping and developing apps and AI agents" (no trailing full stop) — use "Scoping" not "Designing" (user means PM/UX requirements work, not visual UI; "shaping" rejected as too jargony); explicitly NOT "Originally a…" / "Now designing…" / "Technical Product Manager" / "end-to-end"; e2e and unit tests import `siteConfig` to prevent copy drift/reverts.
- Seven homepage dropdowns via `AnimatedDetails` + `DetailsAccordion`: 3 projects (Enterprise-Grade Agents, Pro Animation Tool, Expo Offline Navigation) + 4 jobs with role-focused titles (Working with Artists, On-device AI Agent, Geospatial Machine Learning, Doubled Engineering Speed) in gap-separated lists (`home__project-groups` / `home__experience-groups`, no Projects/Experience section headings); education is a static muted intro-role line below jobs (`BSc in Product, from Aston, UK`, `home__line--role`, not a dropdown); each accordion gets one minimal label-cued schematic tile at top of body (sparse ink strokes, one rose accent, ~10 strokes — Yang & Fan category-drawing style; not photos/screenshots); schematic tile motion pure CSS `@keyframes` only (no new animation libraries like Motion/Framer Motion), gated by accordion open state; work-ex company/product names live in body links with `seoName` for SEO (Pluto, OnDevice, Kawa Space, Aula Education); `+`/`×` disclosure; dropdown bodies follow link-first then pullquote order; pullquotes (`.home__project-body-pullquote`) keep rose left border + padding on all breakpoints including phone and iPad — no mobile override stripping border; grouped multi-link rows inline with · separators (e.g. Pluto · Magic Batch · Create); inline body hyperlinks over separate "View project" rows; Apple HIG accordion via CSS grid `0fr`↔`1fr` (`home__details--opening`/`--closing`), 350ms ease-out open / 250ms ease-in close for height and opacity; timing in `lib/motion.ts`; no JS inline height or measuring mode; user explicitly does NOT want `prefers-reduced-motion` to disable site animations.
- Work-ex and project dropdown body copy must be full text from Google Doc CV (`docs/Raashi Shah CV '26.md`) or existing project content — never short placeholders; no em dashes or sentence-ending full stops in homepage copy; use "Doubled" not "2x-ed" for multipliers; preserve user's copy tone, don't rewrite stories heavily; UK English spellings — do not Americanise; header contact labels (`email me`, `let's meet sometime`) and footer `footerDiscoveryHint` intentionally casual lowercase — leave alone; dropdown bodies and sub-page copy should use correct grammar and title casing per HIG; user edits copy in `content/portfolio.ts` (projects/jobs/education) and `lib/metadata.ts` (intro role/tagline); when user provides copy changes in chat, propagate everywhere relevant (`lib/site-seo.ts`, sub-page content like `content/ondevice.ts`, e2e tests) for consistency; illustrative visuals (schematics, diagrams) for a project must reflect its actual real-world mechanics — check live demos/CVs first, not a generic abstract template; never use AI image-generation tools for site visuals/mockups — schematics and design work must be built in code (SVG/CSS), not generated images.
- **Never revert user's manual content edits** — only fix syntax/build errors and spelling mistakes; when committing, push their full working file changes, not just lines from the chat.

## Learned Workspace Facts

- Homepage is `components/SimpleHome.tsx` via `app/page.tsx` — `SimpleHome` and `ProjectPage` share `components/PortfolioShell.tsx` wrapping `components/SiteShell.tsx` (footer nav + `.home__footer-meta` with coral mark img and `.home__footer-meta-hint` (`footerDiscoveryHint` from `content/site.ts`) — no year; mobile left-aligned within home padding, without HTML width/height attrs on img); project/job content from `content/portfolio.ts` (`projects`, `workExperience`); header contact from `content/site.ts` (`linksById`, `emailLink`, `calendlyLink`, `nameEasterEggHref`, `footerDiscoveryHint`; `raashishah.work@gmail.com`, Calendly `https://calendar.app.google/CQiAZnhWLZs1HF8X6`, SoundCloud `https://m.soundcloud.com/raashi-shah`, Twitter `https://x.com/rash_driving` in footer); `footerLinkOrder` in `content/site.ts` (LinkedIn → Giphy); responsive grid (1 col <40rem, 2 col ≥40rem) with `projects` (3) and `workExperience` (4) as seven `AnimatedDetails` dropdowns plus static `educationLabel` line, footer icons via `components/SocialIcon.tsx` + `lib/footer-social-icons.ts` + `lib/legacy-social-icons.ts` and `@fortawesome/fontawesome-free` brands CSS in `app/layout.tsx`; footer stacks on mobile, single row ≥40rem in `app/styles/footer.css`; footer meta left alignment within home padding covered by `assertFooterMetaWithinHomePadding` in `e2e/helpers/mobile-layout.ts`; accordion timing in `lib/motion.ts` synced to CSS tokens; styles split across `app/styles/` (tokens, layout, accordion, footer) imported by `app/globals.css` (Apple tinted neutrals, 8pt spacing scale, 44px touch targets, safe-area insets, motion tokens); layout measure tokens in `tokens.css` — `--home-measure-narrow` (34ch tagline/left column), `--home-measure-body` (50ch, scoped to `.home__work` + `.home__sheet-body` accordion bodies only, not `.home__primary` detail panel), `--home-detail-viewport-offset` (split panel max-height); `viewport` export in `app/layout.tsx`.
- Core stack: Next.js App Router + Vitest (`npm test`) + Playwright e2e (`e2e/home.spec.ts`); npm package `raashishah.com`; runtime deps Next/React only (GSAP, Lenis, Three.js removed); internal page links must use `next/link` not `<a href>` (ESLint `@next/next/no-html-link-for-pages` fails Vercel production builds); local dev often on `http://localhost:3002` when port 3000 is taken.
- Project/job copy in `content/portfolio.ts` (consumed by `SimpleHome.tsx` and `lib/site-seo.ts`; sourced from `docs/Raashi Shah CV '26.md`); Aula Education body link href is `https://www.aula.education` (not LinkedIn); `INLINE_LINK_SEPARATOR` in `content/types.ts`; `content/site.ts` exports `linksById` map (no `SocialLink.icon` field); old pink static site, rashOS scroll-journey, `/project/[slug]` pages, and Spotify API routes removed from repo; Enterprise-Grade Agents project is the Admissions app (live at admissions.raashishah.com, scoreboard demo at admissionsdemo.raashishah.com/app/scoreboard) — a ranked scoreboard with score and workflow stage (e.g. under review/accepted/offer sent); Doubled Engineering Speed (Aula) refers to a kanban board tool (to do/doing/done) the user introduced for bug/ticket tracking, not a retention metric; Working with Artists (Pluto) core mechanic is cross-platform NFT minting across wallets/platforms.
- Expression (Colourer) and OnDevice content in `content/expression.ts` and `content/ondevice.ts`; routes at `/expression` and `/ondevice` (`app/*/page.tsx` + `ProjectPage`) remain for SEO/direct links but target UX is in-place bottom sheet (phone) + split view (desktop/iPad ≥40rem) from homepage; sub-page metadata via `buildSubpageMetadata` in `lib/metadata.ts`; Expression title "Pro Animation Tool" with "Book demo" Calendly CTA via `ProjectPage` `showBookDemo` + `.home__link--cta` (old rose `--accent` default, `--ink` on hover/focus/active; not "Book Demo"); OnDevice intro role "Health App" with homepage job title "On-device AI Agent"; homepage project/job links internal not GitHub/Twitter; colourer repo is private — never link it publicly; no separate expression subdomain; OnDevice health app's concrete features include glucose alerts and medication reminders; detail-panel architecture uses Next.js intercepting routes + parallel `@detail` slot — `@detail` renders in `app/layout.tsx` as sibling after `DetailProvider` (not passed into client `DetailProvider`); `@detail` slot pages (`app/@detail/expression/page.tsx`, `app/@detail/ondevice/page.tsx`, intercept `(.)*` routes) return null — `DetailPanel` renders client-side via `DetailProvider`; intercept when `isHomepageMounted() && isDetailPath(pathname)` — never `detail !== null` (Next.js always passes a React node for the slot); detail links use `scroll={false}`; gate panel render until media-query ready; HIG-compliant phone sheet (medium/large detents — large `82dvh`, grabber, swipe-dismiss, leading Close, auto-expands medium→large on sheet-body scroll) and desktop/iPad split (`PortfolioShell` left column, tagline fixed, `aria-current` selection; fixed `0.92fr / 1.08fr` grid — no ratio swap on detail open; detail content contained in left column with `min-width: 0` on work column); dismiss split via `requestCloseDetail()` → `finishDetailClose()` — coordinated 250ms exit (`PANEL_CLOSE_MS`): grid collapse (`home__content--detail-closing`), detail height collapse (`home__detail-shell--closing` 0fr grid), and fade (`home__detail--exit`) in parallel; accordion + panel close via `Promise.all` in `DetailsAccordion`/`AnimatedDetails`; `router.back()` on `animationend`/`transitionend` (timeout fallback only), not blind timeout; opening another accordion dismisses split detail — list browsing takes priority.
- Git remote github.com/raashishah/raashishah.github.io (workspace: raashishah.com repo).
- Production deploys on Vercel (project `raashishah`, linked to GitHub); push to `main` triggers deploy.
- Root domain DNS is on Squarespace (`@` A → `76.76.21.21` only; `www` CNAME → `cname.vercel-dns.com`); remove stale Firebase A records if apex serves the old site; subdomains like admissions can stay on Firebase.
- Continual-learning via `agents-memory-updater` subagent + incremental transcript index (`.cursor/hooks/state/continual-learning-index.json`); durable prefs/facts only.
- Rebuild content sourced from Second Brain wiki (/Users/raash/Documents/Second Brain).
- Site metadata in `lib/metadata.ts` (creator "Raashi Shah", `twitterHandle` `@rash_driving`, canonical intro fields); SEO/llms in `lib/site-seo.ts` — `seoConfig.ogTitle` (`siteConfig.name`) for Open Graph/Twitter title, `seoConfig.title` for browser tab (`Raashi Shah | Product Manager, AI Engineer` via `siteConfig.introRole`), `seoConfig.description` uses canonical `siteConfig.description` (`${introRole}. ${introTagline}`); `buildLlmsTxt()` / `buildLlmsFullTxt()` served at `/llms.txt` and `/llms-full.txt` with hire-focused sections (`## Instructions`, `## Open to`, `## Why hire`, `## What she builds`) and Google Calendar wording (not "Calendly"); sitemap includes llms routes; favicon wired in `app/layout.tsx`; OG share image is static `app/opengraph-image.png` (1200×630 Figma export) with metadata URLs at `/opengraph-image.png` (not `/opengraph-image`) in `app/layout.tsx` and sub-pages — code-generated `opengraph-image.tsx` / `OgImage.tsx` / `lib/og-fonts.ts` removed; `nameEasterEggHref` Spotify track in `content/site.ts`.
- `PRODUCT.md` and `DESIGN.md` at project root document product purpose and design system (Apple HIG motion/spacing) for agent context.
- Intro copy lives in `siteConfig` (`lib/metadata.ts`); later polish commits have accidentally reverted it — tests should import `siteConfig` not hardcode strings.

## Learned User Preferences

- **Do not talk to the user in more than 1 sentence at a time unless fully required; when it is required, you can use up to 5 sentences.**
- Prefers simple English; user is a PM transitioning to consulting.
- Build the full site in one pass — no v1/v2 phasing; Next.js App Router confirmed.
- Exclude Bible for Bad People and Kotak Securities from public listings; preserve legacy social links; footer brand icons via Font Awesome `<i class="fa-brands">` HTML (not react-fontawesome) with legacy inline SVGs for Giphy and Medium in `lib/legacy-social-icons.ts` (not in FA free / FA 7 changed Medium).
- Prefers agent execute deployment, setup, QA (`/gstack-qa`), and design review (`/gstack-design-review`) itself — not ask user to manually test or follow step-by-step instructions; use Composer (not Sonnet) for subagents.
- Abandoned rashOS scroll-journey, soft-brutalism, and separate Expression subdomain/site — wants extreme simplicity instead ("fuck all this"); `/expression` and `/ondevice` reuse homepage shell and `.home__*` layout via `SiteShell` + `ProjectPage` (header, contact line, footer); static section bodies, no accordion dropdowns on sub-pages.
- Homepage layout: near-white eggshell background; header at all breakpoints with Raashi Shah (Spotify track easter egg, old rose on hover) plus contact line `email me` / `or` / `let's meet` (Calendly); Twitter in footer not header; LinkedIn first in footer; mobile footer single-row brand icons (`space-between`, labels visually hidden); desktop footer text labels only; no dropdown body indent on phone; 68rem max-width shell; responsive grid; no preloader, scroll journey, Lenis, custom cursor, skip-to-content link, or decorative chrome; favicon legacy rose coral SVG at `public/img/favicon.svg` (#C08081); OG share image is static Figma PNG at `app/opengraph-image.png` (1200×630) — code-generated OG abandoned.
- Typography and links: self-hosted Satoshi variable font sitewide; body 400, UI labels medium (500); intro role muted tertiary, tagline at headline scale; readable clamp() sizes; dropdown body inline links (`.home__inline-link`) grey, medium weight, no underline, typographic Satoshi `↗` via `ExternalLinkArrow` (~1–2pt larger, baseline-aligned inline with text — not inline SVG or thick stroke icons), always old rose (`--accent` via `.home__inline-link-icon`); link text old rose on hover/focus; grouped multi-link `·` separators use old rose accent (`.home__inline-separator`); internal page links keep ↗ but navigate in same tab (`next/link`, no `target="_blank"`); header/footer links keep their own styles.
- Homepage intro copy from `siteConfig` in `lib/metadata.ts`: `introRole` "Technical Product Manager, AI Engineer" + `introTagline` "Designing and developing apps and AI agents end-to-end" — explicitly NOT "Originally a…" / "Now designing…"; e2e and unit tests import `siteConfig` to prevent copy drift/reverts.
- Seven homepage dropdowns via `AnimatedDetails` + `DetailsAccordion`: 3 projects (Entreprise-grade Agents, Professional Tool for Animators, Offline Expo Navigation) + 4 jobs with role-focused titles (Working with Artists, On-device AI, Geospatial Machine Learning, Doubled Engineering Speed) in gap-separated lists (`home__project-groups` / `home__experience-groups`, no Projects/Experience section headings); education is a static muted intro-role line below jobs (`BSc in Product, from Aston, UK`, `home__line--role`, not a dropdown); work-ex company/product names live in body links with `seoName` for SEO (Pluto, OnDevice, Kawa Space, Aula Education); `+`/`×` disclosure; dropdown bodies follow link-first then pullquote order; grouped multi-link rows inline with · separators (e.g. Pluto · Magic Batch · Create); inline body hyperlinks over separate "View project" rows; Apple HIG accordion via CSS grid `0fr`↔`1fr` (`home__details--opening`/`--closing`), 350ms ease-out open / 250ms ease-in close for height and opacity; timing in `lib/motion.ts`; no JS inline height or measuring mode; user explicitly does NOT want `prefers-reduced-motion` to disable site animations.
- Work-ex and project dropdown body copy must be full text from Google Doc CV (`docs/Raashi Shah CV '26.md`) or existing project content — never short placeholders; no em dashes or sentence-ending full stops in homepage copy; use "Doubled" not "2x-ed" for multipliers; preserve user's copy tone, don't rewrite stories heavily; user edits copy in `SimpleHome.tsx` directly.
- **Never revert user's manual content edits** — only fix syntax/build errors and spelling mistakes; when committing, push their full working file changes, not just lines from the chat.

## Learned Workspace Facts

- Homepage is `components/SimpleHome.tsx` via `app/page.tsx` — composes shared `components/SiteShell.tsx`; project/job content from `content/portfolio.ts` (`projects`, `workExperience`); header contact from `content/site.ts` (`linksById`, `emailLink`, `calendlyLink`; `raashishah.work@gmail.com`, Calendly `https://calendly.com/raashishah-work/new-meeting`, Twitter `https://x.com/rash_driving` in footer); responsive grid (1 col <40rem, 2 col ≥40rem) with `projects` (3) and `workExperience` (4) as seven `AnimatedDetails` dropdowns plus static `educationLabel` line, footer for remaining `socialLinks` (LinkedIn first) with icons via `components/SocialIcon.tsx` + `lib/footer-social-icons.ts` + `lib/legacy-social-icons.ts` and `@fortawesome/fontawesome-free` brands CSS in `app/layout.tsx`; accordion timing in `lib/motion.ts` synced to CSS tokens; styles in `app/globals.css` (Apple tinted neutrals, 8pt spacing scale, 44px touch targets, safe-area insets, motion tokens); `viewport` export in `app/layout.tsx`.
- Core stack: Next.js App Router + Vitest (`npm test`) + Playwright e2e (`e2e/home.spec.ts`); npm package `raashishah.com`; runtime deps Next/React only (GSAP, Lenis, Three.js removed); internal page links must use `next/link` not `<a href>` (ESLint `@next/next/no-html-link-for-pages` fails Vercel production builds); local dev often on `http://localhost:3002` when port 3000 is taken.
- Project/job copy in `content/portfolio.ts` (consumed by `SimpleHome.tsx` and `lib/site-seo.ts`; sourced from `docs/Raashi Shah CV '26.md`); Aula Education body link href is `https://www.aula.education` (not LinkedIn); `INLINE_LINK_SEPARATOR` in `content/types.ts`; `content/site.ts` exports `linksById` map (no `SocialLink.icon` field); old pink static site, rashOS scroll-journey, `/project/[slug]` pages, and Spotify API routes removed from repo.
- Expression (Colourer) and OnDevice marketing pages at `/expression` and `/ondevice` (`app/*/page.tsx`, `components/*Page.tsx`, `content/*.ts`) — minimal layout, not homepage clones, both in sitemap; Expression homepage title "Professional Tool for Animators"; OnDevice homepage title "On-device AI" with body link "Agentic Health App" → `/ondevice`; homepage project/job links internal not GitHub/Twitter; colourer repo is private — never link it publicly; no separate expression subdomain.
- Git remote github.com/raashishah/raashishah.github.io (workspace: raashishah.com repo).
- Production deploys on Vercel (project `raashishah`, linked to GitHub); push to `main` triggers deploy.
- Root domain DNS is on Squarespace (`@` A → `76.76.21.21` only; `www` CNAME → `cname.vercel-dns.com`); remove stale Firebase A records if apex serves the old site; subdomains like admissions can stay on Firebase.
- Continual-learning via `agents-memory-updater` subagent + incremental transcript index (`.cursor/hooks/state/continual-learning-index.json`); durable prefs/facts only.
- Rebuild content sourced from Second Brain wiki (/Users/raash/Documents/Second Brain).
- Site metadata in `lib/metadata.ts` (creator "Raashi Shah", `twitterHandle` `@rash_driving`, canonical intro fields); favicon wired in `app/layout.tsx`; OG share image is static `app/opengraph-image.png` (1200×630 Figma export) with metadata URLs at `/opengraph-image.png` (not `/opengraph-image`) in `app/layout.tsx` and sub-pages — code-generated `opengraph-image.tsx` / `OgImage.tsx` / `lib/og-fonts.ts` removed; `nameEasterEggHref` Spotify track in `content/site.ts`.
- `PRODUCT.md` and `DESIGN.md` at project root document product purpose and design system (Apple HIG motion/spacing) for agent context.
- Intro copy lives in `siteConfig` (`lib/metadata.ts`); later polish commits have accidentally reverted it — tests should import `siteConfig` not hardcode strings.

## Learned User Preferences

- Ship straight to `main` — no PR workflow, no local dev server, no unit tests (`npm test`) before commit/push; Vercel production build is the gate.
- Build the full site in one pass — no v1/v2 phasing; Next.js App Router confirmed.
- Exclude Bible for Bad People and Kotak Securities from public listings; preserve legacy social links; footer brand icons via Font Awesome `<i class="fa-brands">` HTML (not react-fontawesome) with legacy inline SVGs for Giphy and Medium in `lib/legacy-social-icons.ts` (not in FA free / FA 7 changed Medium); `SocialIcon.tsx` adds `.home__footer-icon--giphy` (~1.16× scale) and `.home__footer-icon--medium` (~1.06×) for visual parity with FA icons.
- Prefers agent execute deployment, setup, QA (`/gstack-qa`), and design review (`/gstack-design-review`) itself — not ask user to manually test or follow step-by-step instructions; use Composer for subagents (not Composer Fast or Sonnet).
- Publish Cursor/Codex agent skills as one GitHub repo per skill (not npm); install via `npx skills add owner/repo -g -y` or clone to `~/.cursor/skills/`; site links go to each repo README.
- Abandoned rashOS scroll-journey, soft-brutalism, and separate Expression subdomain/site — wants extreme simplicity instead ("fuck all this"); Expression detail opens in-place via Apple HIG bottom sheet on phone and split view on desktop/iPad (use left whitespace; sheet slides up, split enters from left — mindful motion); split view open/close must stay within the allotted left column — right project list must not shift or get overlapped; desktop `.home__primary` capped at tagline width (`--home-measure-narrow` / 34ch) — detail panel must not bleed or expand into project list; split-view dismiss must feel HIG-compliant — one coordinated beat (grid, height, fade in parallel), not fade-then-snap or sequential accordion-then-panel close; not as separate page navigation; legacy `/expression` redirects to `/?detail=expression` — no standalone page routes; static section bodies, no accordion dropdowns in overlays; prefer CSS layout tokens over hardcoded ch/rem in styles — `--home-measure-narrow`, `--home-measure-body`, `--home-detail-viewport-offset` in `tokens.css`.
- Homepage layout: near-white eggshell background; header at all breakpoints with coral mark left of **Decavalent** site name (Spotify track easter egg on `.home__name-link`) — brand is `Decavalent` only, not “Decavalent Labs”; brand link defaults to `--ink`, `--accent` on hover/active; plus contact line `email me` / `or` / `let's meet sometime` (Cal.com `https://cal.com/raashishah`) as inline text links (no padded 44px inline-flex hit boxes); Twitter not in header; footer social order LinkedIn → Twitter → GitHub → Medium → Spotify → SoundCloud (Duolingo/Giphy removed from footer; email/Cal.com header only); footer icons-only at all breakpoints (labels visually hidden via `.home__footer-label`, 44px touch targets); `.home__footer-row` — phone stacked centred, desktop/iPad ≥40rem one row with social icons left and discovery hint right (`justify-content: space-between`; `footerDiscoveryHintBefore` + inline coral mark + `footerDiscoveryHintAfter` — `something's hidden` · mark · `on this page` in `content/site.ts`); `.home__footer-mark` and header mark share one brand asset — `coral` in `content/site.ts` (`public/img/coral.svg`, #C08081) — `1.0625rem` square in footer (`object-fit: contain`), `display: inline-block` with `vertical-align: -0.06em` for optical alignment within hint text; no HTML width/height on `SiteShell` img; favicon/apple-icon use site eggshell `--bg` with rounded corners + coral mark; `.home__intro` gap `--space-2` between identity and tagline; no dropdown body indent on phone; 68rem max-width shell; responsive grid; no preloader, scroll journey, Lenis, custom cursor, skip-to-content link, or decorative chrome; OG share image is static Figma PNG at `app/opengraph-image.png` (1200×630) — code-generated OG abandoned.
- Typography and links: self-hosted Satoshi variable font sitewide; body 400, UI labels medium (500); homepage intro identity at **body scale**, regular weight (400) — smaller than header Decavalent, larger than tagline; `.home__intro-name` in `--accent` includes the comma after the name; `.home__intro-role` at weight 400; tagline at headline scale (medium 500), subline body 400; project accordion titles regular (400), not medium; work-group section headings (Agents, Machine Learning, etc.) old rose (`--accent`); Apple Dictionary masthead above intro uses **Newsreader** for lexical content (headword, definitions, examples, origin) and **Satoshi** for metadata (pronunciation, POS, grammar, inline annotations) — UK IPA only, no US pronunciation; macOS Dictionary scale tokens in `app/styles/dictionary.css`; dictionary standalone at top on desktop/iPad with work list aligned to intro identity; readable clamp() sizes; dropdown body inline links (`.home__inline-link`) grey, medium weight, no underline, typographic Satoshi `↗` via `ExternalLinkArrow` (~1–2pt larger, baseline-aligned inline with text — not inline SVG or thick stroke icons), always old rose (`--accent` via `.home__inline-link-icon`); Expression detail links use `InlineLinkArrow` — ↑ on phone, → on desktop/iPad (≥40rem); link text old rose on hover/focus; grouped multi-link `·` separators use old rose accent (`.home__inline-separator`); other internal page links keep ↗ but navigate in same tab (`next/link`, no `target="_blank"`); header/footer links keep their own styles; clicking outside an open project accordion runs the existing close animation (reuse `lib/motion.ts` tokens — do not recreate).
- Homepage intro copy from `siteConfig` in `lib/metadata.ts`: `introName` "Raashi Shah" + `introRole` "AI Engineer" rendered as split spans in one title line (`introIdentity` "Raashi Shah, AI Engineer" kept for SEO/metadata); `introTagline` "Developing apps and agents"; `introSubline` "Seven years as a Technical Product Manager working with human engineers, over a year with agent engineers" (UK English, no trailing full stop, no em dashes, no "Scoping"); homepage has no standalone role line; e2e and unit tests import `siteConfig` to prevent copy drift/reverts.
- Eleven homepage dropdowns via `AnimatedDetails` + `DetailsAccordion`: 7 projects (Pocket Analyst at sexypa.raashishah.com — heading only, no duplicate body name/href, pullquote "Analytics for traditional business owners, inside GPT", `seoName` "Pocket Analytics"; Vedic Astrologer / Astrothunder at astrothunder.life with body link "Astrothunder", `seoName` "Astrothunder"; Entreprise-grade / admissions at admissions.raashishah.com with body link "Academic admissions cycle", pullquote "Made with Google ADK", `seoName` "Admission Evaluation Agent"; Inventory mgmt / Pink Depot at pinkdepot.raashishah.com with body link "Pink Depot"; Skills last in Agents group — inline row Apple HIG · User Call (github.com/raashishah/apple-hig, github.com/raashishah/user-call) with no pullquote; Animation with body link to `/?detail=expression`; Expo map with pullquote link "After loading, stays available offline") + 4 jobs (On-device in Agents group with body link "Twitter" → x.com/useOnDevice; Working with Artists; Geospatial with pullquote "Built pre-GPT 3"; Doubled engineering speed — sentence case, not title case on "engineering speed") in HIG grouped lists (`workGroupSpecs` in `content/portfolio.ts` — Agents / Machine Learning / Web apps / Product Management via `.home__list-section-heading`, gap `--space-4` on `.home__project-groups`; mix of projects and jobs; no Projects/Experience section headings); education is a static muted intro-role line below jobs (`BSc in Product, from Aston, UK`, `home__line--role`, not a dropdown); each accordion gets one minimal label-cued schematic tile at top of body (sparse ink strokes, one rose accent, ~10 strokes — Yang & Fan category-drawing style; not photos/screenshots); schematic tile motion pure CSS `@keyframes` only (no new animation libraries like Motion/Framer Motion), gated by accordion open state; work-ex company/product names live in body links with `seoName` for SEO (Pluto, OnDevice, Kawa Space, Aula Education); Aula job body link "Aula Education, UK, US"; `+`/`×` disclosure; dropdown bodies follow link-first then pullquote order; pullquotes (`.home__project-body-pullquote`) keep rose left border + padding on all breakpoints including phone and iPad — no mobile override stripping border; grouped multi-link rows inline with · separators (e.g. Pluto · Magic Batch · Create); inline body hyperlinks over separate "View project" rows; Apple HIG accordion via CSS grid `0fr`↔`1fr` (`home__details--opening`/`--closing`), 350ms ease-out open / 250ms ease-in close for height and opacity; timing in `lib/motion.ts`; no JS inline height or measuring mode; user explicitly does NOT want `prefers-reduced-motion` to disable site animations.
- Work-ex and project dropdown body copy must be full text from Google Doc CV (`docs/Raashi Shah CV '26.md`) or existing project content — never short placeholders; no em dashes or sentence-ending full stops in homepage copy; use "Doubled" not "2x-ed" for multipliers; preserve user's copy tone, don't rewrite stories heavily; UK English spellings — do not Americanise; header contact labels (`email me`, `let's meet sometime`) and footer `footerDiscoveryHint` intentionally casual lowercase — leave alone; dropdown bodies and detail-panel copy should use correct grammar and title casing per HIG; user edits copy in `content/portfolio.ts` (projects/jobs/education) and `lib/metadata.ts` (intro role/tagline); when user provides copy changes in chat, propagate everywhere relevant (`lib/site-seo.ts`, detail content like `content/expression.ts`, e2e tests) for consistency; illustrative visuals (schematics, diagrams) for a project must reflect its actual real-world mechanics — check live demos/CVs first, not a generic abstract template; never use AI image-generation tools for site visuals/mockups — schematics and design work must be built in code (SVG/CSS), not generated images.
- **Never revert user's manual content edits** — only fix syntax/build errors and spelling mistakes; when committing, push their full working file changes, not just lines from the chat.

## Learned Workspace Facts

- Homepage is `components/SimpleHome.tsx` via single `app/page.tsx` — `DecavalentDictionary` (`components/DecavalentDictionary.tsx`, copy in `content/decavalent.ts`, styles `app/styles/dictionary.css`) standalone masthead on desktop/iPad; `HomeIntroProfile` (`components/HomeIntroProfile.tsx`) merges intro identity, Cursor avatar (`cursorProfile.avatar` at `/img/raashi.jpg`), subline, and heatmap-only link (`CursorHeatmapSvg` + `content/cursor-heatmap.ts` — no name/handle/arrow row); `HomePortrait` (`content/site.ts` `homePortrait` at `/img/rambo.jpg`, Rambo the cat) + `AgentMentoring` (`components/AgentMentoring.tsx`, ₹2,500/hr, Stripe checkout via `POST /api/mentoring/checkout`, no quantity selector) in left column on desktop; heatmap links out to cursor.com/@rashdriving (no iframe — Cursor blocks embeds); `PortfolioShell` wraps `SiteShell` (header brand from `siteConfig.name` = Decavalent; footer `.home__footer-row` — social nav left + `.home__footer-meta` discovery hint right on desktop/iPad, stacked on phone; inline coral mark inside `.home__footer-meta-hint` via `footerDiscoveryHintBefore`/`footerDiscoveryHintAfter` from `content/site.ts` — no year); project/job content from `content/portfolio.ts` (`projects`, `workExperience`, `workGroupSpecs`); header contact from `content/site.ts` (`linksById`, `emailLink`, `calendlyLink`, `nameEasterEggHref`, `footerDiscoveryHintBefore`/`footerDiscoveryHintAfter`/`footerDiscoveryHint`; `raashishah.work@gmail.com`, Cal.com `https://cal.com/raashishah`, Twitter `https://x.com/rash_driving` in header/footer as applicable); `footerLinkOrder` in `content/site.ts` (LinkedIn → Twitter → GitHub → Medium → Spotify → SoundCloud — Duolingo/Giphy removed from footer); footer icons-only at all breakpoints (`.home__footer-label` visually hidden) via `components/SocialIcon.tsx` + `lib/footer-social-icons.ts` + `lib/legacy-social-icons.ts` and `@fortawesome/fontawesome-free` brands CSS in `app/layout.tsx`; responsive grid (1 col <40rem, 2 col ≥40rem) with eleven `AnimatedDetails` dropdowns grouped via `resolveWorkGroups`; footer meta alignment within home padding covered by `assertFooterMetaWithinHomePadding` in `e2e/helpers/mobile-layout.ts`; accordion timing in `lib/motion.ts` synced to CSS tokens; styles split across `app/styles/` (tokens, layout, accordion, footer, dictionary, mentoring, panel) imported by `app/globals.css`; layout measure tokens in `tokens.css` — `--home-measure-narrow`, `--home-measure-body`, `--home-detail-viewport-offset`; favicon/apple-icon use site eggshell `--bg` with rounded corners + coral mark; `viewport` export in `app/layout.tsx`.
- Core stack: Next.js App Router + Vitest (`npm test`) + Playwright e2e (`e2e/home.spec.ts`); npm package `raashishah.com`; runtime deps Next/React only (GSAP, Lenis, Three.js removed); internal page links must use `next/link` not `<a href>` (ESLint `@next/next/no-html-link-for-pages` fails Vercel production builds); local dev often on `http://localhost:3002` when port 3000 is taken.
- Project/job copy in `content/portfolio.ts` (consumed by `SimpleHome.tsx` and `lib/site-seo.ts`; sourced from `docs/Raashi Shah CV '26.md`); display titles vs `seoName` differ (e.g. Pocket Analyst / "Pocket Analytics", Vedic Astrologer / "Astrothunder", Entreprise-grade / "Admission Evaluation Agent", Animation / "Expression"); Aula job title "Doubled engineering speed" (sentence case), body link "Aula Education, UK, US" → `https://www.aula.education`; `INLINE_LINK_SEPARATOR` in `content/types.ts`; `content/site.ts` exports `linksById` map (no `SocialLink.icon` field); old pink static site, rashOS scroll-journey, `/project/[slug]` pages, and Spotify API routes removed from repo; Pocket Analyst is Sexy PA (live at sexypa.raashishah.com — Tally financial intelligence via ChatGPT MCP); Vedic Astrologer is Astrothunder (live at astrothunder.life); Entreprise-grade is the Admissions app (live at admissions.raashishah.com, scoreboard demo at admissionsdemo.raashishah.com/app/scoreboard); Doubled engineering speed (Aula) refers to a kanban board tool (to do/doing/done) the user introduced for bug/ticket tracking, not a retention metric; Working with Artists (Pluto) core mechanic is cross-platform NFT minting across wallets/platforms; On-device job links to Twitter (x.com/useOnDevice), no detail panel.
- Expression (Colourer) detail content in `content/expression.ts`; only Expression has an in-place detail panel — `/?detail=expression` search param via `DetailProvider` + `lib/detail-routes.ts`; legacy `/expression` 308-redirects in `next.config.ts`; no standalone page routes (single `app/page.tsx`); Expression homepage accordion title "Animation" with "Try now" CTA linking to Google Drive demo folder (not "Book demo"); colourer repo is private — never link it publicly; no separate expression subdomain; detail-panel architecture: client `DetailProvider` reads `detail` search param, `DetailPanel`/`ProjectDetail` render in `PortfolioShell` left column; HIG-compliant phone sheet (medium/large detents — medium `70dvh`, large `82dvh`, grabber, swipe-dismiss, leading Close, auto-expands medium→large on sheet-body scroll) and desktop/iPad split (tagline fixed, `aria-current` selection; detail content contained in left column with `min-width: 0` on work column); dismiss via `requestCloseDetail()` → `finishDetailClose()` — coordinated 250ms exit (`PANEL_CLOSE_MS`): grid collapse, detail height collapse, and fade in parallel; accordion + panel close via `Promise.all` in `DetailsAccordion`/`AnimatedDetails`; `router.replace("/")` on close (timeout fallback only); opening another accordion dismisses split detail — list browsing takes priority; heatmap hidden while expression split is open.
- Git remote github.com/raashishah/raashishah.github.io (workspace: raashishah.com repo).
- Production deploys on Vercel (project `raashishah`, linked to GitHub); push to `main` triggers deploy.
- Root domain DNS is on Squarespace (`@` A → `76.76.21.21` only; `www` CNAME → `cname.vercel-dns.com`); remove stale Firebase A records if apex serves the old site; subdomains like admissions can stay on Firebase.
- Continual-learning via `agents-memory-updater` subagent + incremental transcript index (`.cursor/hooks/state/continual-learning-index.json`); durable prefs/facts only.
- Rebuild content sourced from Second Brain wiki (/Users/raash/Documents/Second Brain).
- Site metadata in `lib/metadata.ts` — `siteConfig.name`/`creator` "Decavalent", canonical URL `https://decavalent.com` (`NEXT_PUBLIC_SITE_URL`/`SITE_URL` override); intro fields still `introName` "Raashi Shah", `introRole` "AI Engineer"; `twitterHandle` `@rash_driving`; SEO/llms in `lib/site-seo.ts` — `seoConfig.ogTitle` (`siteConfig.name`) for Open Graph/Twitter title, `seoConfig.title` for browser tab (`Decavalent | AI Engineer` via `siteConfig.creator` + `introRole`), `seoConfig.description` uses canonical `siteConfig.description`; `buildLlmsTxt()` / `buildLlmsFullTxt()` served at `/llms.txt` and `/llms-full.txt` with hire-focused sections and Cal.com scheduling link (`https://cal.com/raashishah`, not Google Calendar); sitemap includes llms routes; favicon/apple-icon use shared `coral.src` from `content/site.ts` on eggshell `--bg` with rounded corners in `app/layout.tsx`; OG share image is static `app/opengraph-image.png` (1200×630 Figma export) with metadata URLs at `/opengraph-image.png` (not `/opengraph-image`); `nameEasterEggHref` Spotify track in `content/site.ts`.
- `PRODUCT.md` and `DESIGN.md` at project root document product purpose and design system (Apple HIG motion/spacing) for agent context.
- Intro copy lives in `siteConfig` (`lib/metadata.ts`); later polish commits have accidentally reverted it — tests should import `siteConfig` not hardcode strings.

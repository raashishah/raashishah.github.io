DROP

Dark-mode work is scoped to palette tokens while spacing and touch targets stay frozen: `.hig/progress.yaml` sets `brand_mutation_veto: spacing_and_touch_targets_locked`, and `.hig/app-design.md` lists spacing, type scale, weights, and motion as out of scope for the warm dark pass. The `@media (prefers-color-scheme: dark)` block in `app/styles/tokens.css` overrides only ink/surface colour variables, leaving the 8pt `--space-*` scale, `--home-touch-min` (2.75rem), layout measure tokens, and accordion group gaps untouched. Across `app/styles/` (layout, footer, panel, mentoring, dictionary, accordion), gaps, padding, margins, and minimum hit areas consistently use `var(--space-*)` or `var(--home-touch-min)` with no literal hex, `rgb()`, or `hsl()` bound to box-model properties—a repo scan finds no colour value masquerading as a spacing defect. Any dark-mode spacing change would violate the register veto and duplicate layout ownership without fixing a real bug.
